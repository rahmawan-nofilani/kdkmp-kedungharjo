"use client";
import type { ReactNode } from "react";
import { CloseIcon } from "./icons";
import styles from "./ui.module.css";
export function Modal({open,title,description,children,footer,onClose,closeLabel="Tutup"}:{open:boolean;title:string;description?:ReactNode;children:ReactNode;footer?:ReactNode;onClose:()=>void;closeLabel?:string}){if(!open)return null;return <div className={styles.backdrop} role="presentation" onMouseDown={e=>{if(e.currentTarget===e.target)onClose()}}><section className={styles.modal} role="dialog" aria-modal="true" aria-label={title}><header className={styles.overlayHeader}><div><h2>{title}</h2>{description?<p>{description}</p>:null}</div><button className={styles.iconButton} type="button" onClick={onClose} aria-label={closeLabel}><CloseIcon/></button></header><div className={styles.overlayBody}>{children}</div>{footer?<footer className={styles.overlayFooter}>{footer}</footer>:null}</section></div>}
export function Drawer({open,title,description,children,onClose,side="right"}:{open:boolean;title:string;description?:ReactNode;children:ReactNode;onClose:()=>void;side?:"left"|"right"}){if(!open)return null;return <div className={styles.backdrop} role="presentation" onMouseDown={e=>{if(e.currentTarget===e.target)onClose()}}><aside className={`${styles.drawer} ${side==="left"?styles.drawerLeft:""}`} role="dialog" aria-modal="true" aria-label={title}><header className={styles.overlayHeader}><div><h2>{title}</h2>{description?<p>{description}</p>:null}</div><button className={styles.iconButton} type="button" onClick={onClose} aria-label="Tutup"><CloseIcon/></button></header><div className={styles.overlayBody}>{children}</div></aside></div>}
export function Toast({tone="info",title,children}:{tone?:"success"|"warning"|"danger"|"info";title:string;children?:ReactNode}){return <div className={`${styles.toast} ${styles[`${tone}Toast`]}`} role={tone==="danger"?"alert":"status"}><strong>{title}</strong>{children?<div>{children}</div>:null}</div>}
